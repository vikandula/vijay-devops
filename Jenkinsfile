pipeline {
    agent any  // Run on any available agent
    stages {
        stage('Print Message') {
            steps {
                echo 'Hello from Jenkins Pipeline!'
            }
        }
    }
}
